# F1 Driver Telemetry Comparator

A Streamlit web application for corner-by-corner F1 telemetry comparison and animated race replay powered by FastF1 and Plotly.

## Setup & Running

```bash
python -m pip install -r requirements.txt
streamlit run app.py
```
